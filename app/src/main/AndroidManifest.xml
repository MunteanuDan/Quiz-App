<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.ase.tema_quiz_munteanu_dan_cristian">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tema_Quiz_Munteanu_Dan_Cristian">
        <activity android:name=".ExpiredTimeActivity"></activity>
        <activity android:name=".Rezultat3" />
        <activity android:name=".Rezultat2" />
        <activity android:name=".Rezultat1" />
        <activity android:name=".Quiz" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>